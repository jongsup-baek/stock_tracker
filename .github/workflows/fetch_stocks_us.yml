name: US 주식 정보 자동 수집

on:
  schedule:
    # 매일 미국 장 마감 후 (EST 4:30PM = UTC 21:30, 한국 시간 오전 6:30)
    - cron: '30 21 * * 1-5'  # 월-금 UTC 21:30 (미국 장 마감 후)

  workflow_dispatch:  # 수동 실행 가능
    inputs:
      tickers:
        description: '조회할 티커 (쉼표로 구분)'
        required: false
        default: ''

jobs:
  fetch-us-stocks:
    runs-on: ubuntu-latest

    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 패키지 설치
        run: |
          pip install -r requirements.txt

      - name: US 주식 정보 수집
        run: |
          # 수동 실행 시 입력값 우선, 없으면 PORTFOLIO_US.md에서 읽기
          TICKERS="${{ github.event.inputs.tickers }}"
          if [ -z "$TICKERS" ]; then
            echo "Reading tickers from PORTFOLIO_US.md..."
            python stock_fetcher_us.py
          else
            echo "Target tickers: $TICKERS"
            IFS=',' read -ra TICKER_ARRAY <<< "$TICKERS"
            for ticker in "${TICKER_ARRAY[@]}"; do
              echo "Fetching data for ticker: $ticker"
              python stock_fetcher_us.py "$ticker" || echo "Failed to fetch $ticker"
            done
          fi

      - name: Git 설정
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions Bot"

      - name: 변경사항 커밋 및 푸시
        run: |
          git add data/us/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📊 US 주식 데이터 자동 업데이트 - $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          fi
